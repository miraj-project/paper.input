(ns paper-input-test
  (:require [polymer.paper.input :as pinput :refer :all]))

(meta (pinput/addon-behavior))

(meta (var pinput/addon-behavior))

(meta (pinput/behavior))
(meta (pinput/char-counter))
(meta (pinput/container))
(meta (pinput/error))


